# 🚀 Инструкция по деплою

## Подготовка

### 1. Создайте GitHub репозиторий

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/clothing-shop.git
git push -u origin main
```

---

## Backend (Render.com)

### Вариант A: Через GitHub

1. Зайдите на https://render.com
2. Нажмите "New" → "Web Service"
3. Подключите GitHub репозиторий
4. Настройки:
   - **Name**: `clothing-shop-api`
   - **Region**: Frankfurt (EU Central)
   - **Branch**: main
   - **Root Directory**: `backend`
   - **Environment**: Node
   - **Build Command**: `npm install`
   - **Start Command**: `node server.js`
   - **Instance Type**: Free

5. Environment Variables (добавьте все из .env):
   ```
   BOT_TOKEN=ваш_токен
   ADMIN_ID=ваш_id
   PORT=3000
   NODE_ENV=production
   FRONTEND_URL=https://ваш-app.vercel.app
   CLOUDINARY_CLOUD_NAME=...
   CLOUDINARY_API_KEY=...
   CLOUDINARY_API_SECRET=...
   MANAGER_USERNAME=...
   ```

6. Нажмите "Create Web Service"
7. Дождитесь завершения деплоя
8. **Скопируйте URL** (например: `https://clothing-shop-api.onrender.com`)

### Вариант B: Вручную (без Git)

1. Создайте zip архив папки `backend`
2. На Render.com выберите "Deploy from repository" → "Upload"
3. Загрузите архив
4. Следуйте шагам 4-8 выше

---

## Frontend (Vercel)

### Вариант A: Через GitHub

1. Зайдите на https://vercel.com
2. Нажмите "New Project"
3. Импортируйте GitHub репозиторий
4. Настройки:
   - **Framework Preset**: Vite
   - **Root Directory**: `frontend`
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`

5. Environment Variables:
   ```
   VITE_API_URL=https://ваш-backend.onrender.com
   ```

6. Нажмите "Deploy"
7. Дождитесь завершения деплоя
8. **Скопируйте URL** (например: `https://clothing-shop.vercel.app`)

### Вариант B: Через Vercel CLI

```bash
cd frontend
npm install -g vercel
vercel login
vercel
# Следуйте инструкциям
```

---

## Финальная настройка

### 1. Обновите Backend .env на Render

Зайдите в настройки вашего Web Service на Render и обновите:
```
FRONTEND_URL=https://clothing-shop.vercel.app
```

Нажмите "Save Changes" - сервис перезапустится автоматически.

### 2. Настройте Telegram бота

Отправьте @BotFather команды:

```
/setmenubutton
Выберите вашего бота
Вставьте URL: https://clothing-shop.vercel.app
```

---

## Проверка работы

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку меню
3. Должно открыться Mini App с каталогом

### Если не работает:

1. **Проверьте логи Backend на Render:**
   - Зайдите в ваш Web Service
   - Откройте вкладку "Logs"
   - Ищите ошибки

2. **Проверьте логи Frontend на Vercel:**
   - Зайдите в ваш проект
   - Откройте "Deployments"
   - Кликните на последний деплой
   - Посмотрите "Build Logs"

3. **Проверьте переменные окружения:**
   - Убедитесь что все переменные правильно заполнены
   - Особенно `VITE_API_URL` и `FRONTEND_URL`

4. **Проверьте CORS:**
   - Backend должен разрешать запросы с вашего Frontend URL
   - Это настраивается в `server.js` через переменную `FRONTEND_URL`

---

## Обновление приложения

### Backend:

1. Внесите изменения в код
2. Сделайте commit и push:
   ```bash
   git add .
   git commit -m "Update backend"
   git push
   ```
3. Render автоматически задеплоит изменения

### Frontend:

1. Внесите изменения в код
2. Сделайте commit и push:
   ```bash
   git add .
   git commit -m "Update frontend"
   git push
   ```
3. Vercel автоматически задеплоит изменения

---

## Альтернативные хостинги

### Backend:
- **Railway.app** - аналог Render
- **Fly.io** - хороший для Node.js
- **Heroku** - платный, но надёжный

### Frontend:
- **Netlify** - аналог Vercel
- **Cloudflare Pages** - быстрый CDN
- **GitHub Pages** - только для статики

---

## Мониторинг

### Render:
- Бесплатный план засыпает после 15 минут неактивности
- Первый запрос после сна займёт ~30 секунд
- Решение: используйте UptimeRobot для пинга каждые 5 минут

### Vercel:
- Бесплатный план: 100GB bandwidth/месяц
- Обычно этого достаточно для небольшого магазина

---

## Резервное копирование

Регулярно делайте бэкап файлов из `backend/data/`:
- products.json
- users.json
- stats.json

Скачать можно через Render CLI или прямо из репозитория.

---

## Советы по производительности

1. **Оптимизация изображений:**
   - Cloudinary автоматически оптимизирует
   - Используйте WebP формат когда возможно

2. **Кэширование:**
   - Vercel кэширует статику автоматически
   - Для API можно добавить Redis (платно)

3. **CDN:**
   - Vercel использует глобальный CDN
   - Cloudinary тоже имеет CDN

---

## Безопасность

1. **Никогда не коммитьте .env файлы**
2. **Используйте переменные окружения для секретов**
3. **Ограничьте ADMIN_ID только своим ID**
4. **Регулярно обновляйте зависимости:**
   ```bash
   npm update
   npm audit fix
   ```

---

## Масштабирование

Когда приложение вырастет:

1. **База данных**: переходите на MongoDB Atlas
2. **Хранилище**: продолжайте Cloudinary или S3
3. **Backend**: переходите на платный план Render или VPS
4. **Frontend**: Vercel справится с большим трафиком

---

Готово! 🎉 Ваш магазин развёрнут и готов к работе!
